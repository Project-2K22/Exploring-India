import React from 'react';
import BaseContainer from '../components/BaseContainer';

const UserProfile = () => {
    return (
        <BaseContainer w="lg">
            {/* TODO: edit this file only */}
            User Profile
        </BaseContainer>
    );
};

export default UserProfile;
